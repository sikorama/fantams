# Binaires de test / CLI compilés
tests/*_test
ppdump
fantams

# Build CMake
build/

# Sorties générées par les exemples
examples/*.bin
examples/*.sna

# Artefacts WASM (destination par défaut de build-wasm.sh ; un hôte lui passe
# FANTAMS_OUT_DIR pour les recevoir ailleurs)
dist/
*.o
*.wasm.js
